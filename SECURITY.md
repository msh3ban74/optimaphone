# الأمن والخصوصية — أوبتيما فون

## المبدأ

المتجر موقع ساكن (static) بلا خادم تطبيقات وبلا قاعدة بيانات وبلا حسابات.
ما لا يُجمع لا يمكن أن يُسرَّب. هذا هو أقوى ضمان للخصوصية، وعليه بُني المتجر.

---

## ما يُحفظ وما لا يُحفظ

| البيان | الحفظ |
| --- | --- |
| السلة (معرّفات الأصناف والكميات) | على الجهاز — `localStorage` |
| المختارات (معرّفات الأصناف) | على الجهاز — `localStorage` |
| سجل الطلبات (رقم الطلب، الأصناف، المبالغ) | على الجهاز — `localStorage` |
| الاسم والهاتف والمدينة والعنوان | **لا يُحفظ** — في الذاكرة حتى إغلاق الصفحة |
| صورة إيصال التحويل | **لا تُحفظ ولا تُرفع** — في الذاكرة فحسب |
| رقم المُرسِل وقيمة التحويل | **لا يُحفظ** — في الذاكرة فحسب |

بيانات المستلِم تُجرَّد من الطلب قبل حفظه (`stripPersonalData` في
`src/store/stores.ts`). تخرج هذه البيانات مرة واحدة فقط: داخل رسالة الواتساب
التي يرسلها العميل بنفسه بضغطة صريحة.

صفحة **الخصوصية** تتيح للزائر محو كل ما حُفظ على جهازه محوًا تامًا.

---

## سياسة أمن المحتوى

مُعلنة في `index.html` ومُطبَّقة على كل صفحة:

```
default-src 'none'; script-src 'self'; style-src 'self';
img-src 'self' data: blob:; font-src 'self'; connect-src 'self';
media-src 'self'; manifest-src 'self'; worker-src 'self';
base-uri 'none'; form-action 'none'; object-src 'none';
upgrade-insecure-requests
```

- **`default-src 'none'`** — كل ما لم يُصرَّح به مرفوض ابتداءً.
- **لا `'unsafe-inline'` ولا `'unsafe-eval'`** — لا في النصوص البرمجية ولا في
  الأنماط. أُزيلت كل الأنماط المضمَّنة من الواجهة لأجل ذلك؛ حتى ألوان عيّنات
  المنتجات تُضبط عبر واجهة CSSOM لا عبر سمة `style`.
- **`form-action 'none'`** — لا يمكن لأي نموذج أن يرسل بيانات إلى أي وجهة.
- **`base-uri 'none'`** — يمنع حقن وسم `<base>` لإعادة توجيه المسارات.

تُوثَّق حالة الالتزام باختبار متصفح فعلي: تُفتح كل صفحة ويُتحقق من خلو سجل
المتصفح من أي مخالفة.

---

## انعدام الجهات الخارجية

لا يُطلب من الشبكة أي خط أو صورة أو نص برمجي خارج نطاق المتجر:

- **الخطوط**: خطوط النظام العربية (`SF Arabic` و`Segoe UI` و`Noto Sans Arabic`).
  لا اتصال بخدمة خطوط، فلا تسريب لعنوان الزائر ولا انتظار لتحميل خارجي.
- **الصور**: تُستضاف في `public/products/`، ويرفض المتجر أي مسار غير داخلي
  (`safeImagePath`).
- **لا تتبّع ولا تحليلات ولا إعلانات ولا وسوم اجتماعية.**

---

## التحقق من المدخلات

`src/utils/security.ts` هو الحاجز الوحيد الذي تمر منه كل مدخلات المستخدم:

| الإجراء | الغرض |
| --- | --- |
| `sanitizeText` / `sanitizeMultiline` | إزالة محارف التحكم ومحارف قلب اتجاه النص، وحدّ الطول |
| `normalizeEgyptianPhone` | التحقق من بنية الرقم وتوحيد صيغته |
| `parseAmount` | قبول المبالغ الرقمية ضمن حد أعلى |
| `safeImagePath` | رفض الروابط الخارجية ومسارات الصعود `..` |
| `safeHexColor` | قبول القيم اللونية السداسية فحسب |
| `isSafeUrl` | قبول `https` والمسارات الداخلية، ورفض `javascript:` و`data:` |
| `verifyReceiptFile` | التحقق من النوع والحجم **والتوقيع الثنائي** للملف |

**محارف قلب الاتجاه** (`U+202A`–`U+202E` و`U+2066`–`U+2069`) تُزال تحديدًا،
إذ تُستغل في إظهار نص على غير حقيقته داخل الرسائل.

**إيصال التحويل** لا يُقبل بناءً على امتداده ولا على نوعه المُعلن وحدهما، بل
تُقرأ أوائل بايتاته ويُطابق توقيعها توقيع JPEG أو PNG أو WEBP.

---

## سلامة رسائل الواتساب

تُبنى الرسائل من بيانات مُطهَّرة، ويُرمَّز نصها بـ `encodeURIComponent`، ويُصفَّى
رقم الوجهة إلى أرقام محضة قبل تركيب الرابط. فلا سبيل لحقن معاملات في الرابط
عبر ما يكتبه المستخدم.

---

## حماية الواجهة

- **منع التأطير**: يتحقق المتجر عند الإقلاع من أنه ليس داخل إطار، وينتقل بالنافذة
  العليا إليه إن كان كذلك — درءًا لخداع النقر. (`frame-ancestors` لا يعمل إلا
  عبر ترويسة HTTP، وهي غير متاحة على الاستضافة الساكنة، فكان هذا هو السبيل.)
- **الروابط الخارجية**: كلها `rel="noopener noreferrer"`، فلا تصل الصفحة المفتوحة
  إلى نافذة المتجر.
- **`Referrer-Policy: no-referrer`** — لا يُسرَّب مسار الصفحة إلى أي وجهة.
- **حقن النصوص (XSS)**: تُهرَّب كل القيم عبر React، ولا يُستعمل
  `dangerouslySetInnerHTML` في أي موضع من المشروع.

---

## عامل الخدمة

`public/sw.js` يخزّن أصول المتجر فقط، ويرفض تخزين أي طلب من نطاق آخر
(`url.origin !== self.location.origin`). صفحة الدخول تُطلب من الشبكة أولًا حتى
لا تُخدم نسخة قديمة، مع الرجوع إلى المخزن عند انقطاع الاتصال.

---

## حدود صريحة

الأمانة تقتضي بيان ما لا يفعله هذا البناء:

1. **لا تحقق آليًا من التحويلات.** يراجع التاجر الإيصال بنفسه ثم يرسل التأكيد.
   لا يوجد اتصال ببوابة دفع.
2. **صفحة التاجر (`/merchant`) غير محمية بكلمة مرور.** وهي لا تحتوي على أي بيان
   ولا تصل إلى بيانات أحد؛ إنما تصوغ نصًا. من أراد حجبها فليُزل مسارها من
   `src/App.tsx`.
3. **الطلبات لا تُخزَّن مركزيًا.** سجلّ التاجر هو محادثات الواتساب.
4. **حماية `localStorage`** تنتهي عند حدود الجهاز: من يملك الجهاز يرى ما عليه.
   ولهذا لا يُحفظ فيه أي بيان شخصي.

الانتقال إلى خادم حقيقي (حسابات، تحقق آلي من الدفع، سجل مركزي) يستلزم طبقة
خلفية، وهي خارج نطاق البناء الحالي.
